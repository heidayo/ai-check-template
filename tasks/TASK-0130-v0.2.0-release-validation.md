# TASK-0130: v0.2.0 release validation

## メタデータ

| フィールド | 内容 |
|-----------|------|
| TASK-ID   | TASK-0130 |
| SPEC-ID   | SPEC-0035 |
| PLAN-ID   | PLAN-0035 |
| ステータス | Done |
| 担当Agent | Validation |
| 並列可否  | Yes |
| 依存TASK  | TASK-0129 |
| 見積     | 15m |

## 責務

`make validate` の structural guard に v0.2.0 Released 状態の最低限を追加する。

## File Scope（変更許可範囲）

- 変更: `Makefile`
- 削除: なし

## 禁止事項

- runtime source を変更しない
- actual `npm publish` を実行しない
- npm auth material を書かない
- `--no-verify`, `--force`, `rm -rf` を使わない

## 完了条件

- [x] `Makefile` checks README / README-ja / roadmap v0.2.0 Released status
- [x] `Makefile` checks release notes include npm / npx / GitHub Release evidence
- [x] `Makefile` falls back to registry visibility when the current package version is already published
- [x] `make validate-structure` pass
- [x] TASK-0130 採点が 100/S++

## Tests

- `make validate-structure`
- `make validate`

## Error Resolution

| 失敗 | 復旧手順 |
|---|---|
| validation too broad | stable release docs に限定して grep を調整 |
| duplicate publish dry-run failure | current version published check を先に実行し、registry visibility check に切り替える |
| false positive | exact phrase / file scope を narrow |

## Knowledge Management

release docs guard の false positive が発生した場合、maintainer が command, expected, actual, workaround を `sage/failures.md` に記録する。

## 段階採用

追加 guard は docs release completion の最低限に限定し、runtime validation には触れない。

## Done Definition

SPEC-0035 AC-09 の validation path が存在する。

## 実行ログ

| フィールド | 内容 |
|-----------|------|
| RUN-ID    | 2026-05-16-spec-0035-task-0130 |
| 開始     | 2026-05-16 |
| 完了     | 2026-05-16 |
| 結果     | PASS |
| Gate結果  | structural: pass / functional: pass / security: pass |
