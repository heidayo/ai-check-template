# TASK-0137: v0.3.0 release validation

## メタデータ

| フィールド | 内容 |
|-----------|------|
| TASK-ID   | TASK-0137 |
| SPEC-ID   | SPEC-0037 |
| PLAN-ID   | PLAN-0037 |
| ステータス | Done |
| 担当Agent | Validation |
| 並列可否  | Yes |
| 依存TASK  | TASK-0136 |
| 見積     | 20m |

## 責務

`make validate` の structural guard に v0.3.0 release docs / GitHub Actions guide の最低限を追加する。

## File Scope（変更許可範囲）

- 変更: `Makefile`
- 削除: なし

## 禁止事項

- runtime source を変更しない
- workflow / action source を変更しない
- npm publish を実行しない
- secret / auth URL を書かない
- `--no-verify`, `--force`, `rm -rf` を使わない

## 完了条件

- [x] `Makefile` checks v0.3.0 release notes required sections
- [x] `Makefile` checks README / README-ja / roadmap Released status
- [x] `Makefile` checks GitHub Actions guide exact pin / future alias / Marketplace planned wording
- [x] `make validate-structure` pass
- [x] TASK-0137 採点が 100/S++

## Tests

- `make validate-structure`
- `make validate`

## Error Resolution

| 失敗 | 復旧手順 |
|---|---|
| validation too broad | v0.3.0 docs files に限定して grep を調整 |
| false positive | exact phrase / file scope を narrow |
| missing guard | Makefile に section/link/status check を追加 |

## Knowledge Management

release docs guard の false positive が発生した場合、maintainer が command, expected, actual, workaround を `sage/failures.md` に記録する。同じ false positive が 3 回累積した場合は `sage/anti-patterns.md` への昇格候補にする。

## 段階採用

追加 guard は release docs と GitHub Actions guide の最低限に限定し、runtime validation には触れない。

## Done Definition

SPEC-0037 AC-04 の validation path が存在する。

## 実行ログ

| フィールド | 内容 |
|-----------|------|
| RUN-ID    | 2026-05-16-spec-0037-task-0137 |
| 開始     | 2026-05-16 |
| 完了     | 2026-05-16 |
| 結果     | PASS |
| Gate結果  | structural: pass / functional: pass / security: pass |
