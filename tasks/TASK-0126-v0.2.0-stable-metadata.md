# TASK-0126: v0.2.0 stable metadata

## メタデータ

| フィールド | 内容 |
|-----------|------|
| TASK-ID   | TASK-0126 |
| SPEC-ID   | SPEC-0034 |
| PLAN-ID   | PLAN-0034 |
| ステータス | Done |
| 担当Agent | Release |
| 並列可否  | Yes |
| 依存TASK  | none |
| 見積     | 25m |

## 責務

stable `v0.2.0` publish prep に必要な package metadata、validation、test expectation を更新する。

## File Scope（変更許可範囲）

- 変更: `package.json`
- 変更: `Makefile`
- 変更: `tests/cli/init.test.mjs`
- 削除: なし

## 禁止事項

- runtime source を変更しない
- actual `npm publish` を実行しない
- dist-tag mutation を実行しない
- npm auth material を書かない
- `--no-verify`, `--force`, `rm -rf` を使わない

## 完了条件

- [x] `package.json` version is `0.2.0`
- [x] install state test expectation is `0.2.0`
- [x] `Makefile` validates `0.2.0`
- [x] `Makefile` uses `npm publish --dry-run --tag latest --json`
- [x] TASK-0126 採点が 100/S++

## Tests

- `node -e "const p=require('./package.json'); if (p.version !== '0.2.0') process.exit(1)"`
- `rg -n "\"packageVersion\": \"0.2.0\"|packageVersion: \"0.2.0\"" tests/cli/init.test.mjs`
- `npm publish --dry-run --tag latest --json`

## Error Resolution

| 失敗 | 復旧手順 |
|---|---|
| version mismatch | `package.json` と test expectation を同期 |
| dry-run tag mismatch | Makefile の publish preflight を latest に更新 |
| validation fails | failing grep / test output に従い File Scope 内で修正 |

## Knowledge Management

stable publish preflight の失敗が再発した場合、maintainer が npm version, command, expected, actual output を `sage/failures.md` に記録する。

## 段階採用

runtime behavior は維持し、package metadata と validation のみ stable release candidate に更新する。

## Done Definition

SPEC-0034 AC-01, AC-02 と publish dry-run validation が pass する。

## 実行ログ

| フィールド | 内容 |
|-----------|------|
| RUN-ID    | 2026-05-16-spec-0034-task-0126 |
| 開始     | 2026-05-16 |
| 完了     | 2026-05-16 |
| 結果     | PASS |
| Gate結果  | structural: pass / functional: pass / security: pass |
